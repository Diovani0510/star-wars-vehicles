import { VehiclesListPage } from 'pages'
import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'

ReactDOM.render(
  <React.StrictMode>
    <VehiclesListPage />
  </React.StrictMode>,
  document.getElementById('root')
)
